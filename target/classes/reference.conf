{
  // Type your name here
  name: "Daniel Chernis"
  // Input subscriber file type
  type: "CSV"
  // Input file name, valid inputs are "subscriber.json" if "type" is set to "JSON" and
  // "subscriber.csv" if "type" is set to "CSV"
  fileName: "subscriber.csv"
}